📨 Real-Time Notification System (Laravel 10 + Database Queue + WebSockets) This project demonstrates a real-time notification system using **Laravel 10**, **Database Queue**, and **WebSockets** (Pusher protocol / Soketi / Laravel WebSockets). A user submits a message → it is stored in the database → processed in a queue job → then broadcast to all connected clients instantly. This simulates real-world chat apps, order tracking, live updates, dashboards, and event-driven systems. --- ## 🚀 Features ### ✔ 1. Message API (POST /api/messages) - Accepts sender_id and message - Stores message in the database - Dispatches a queue job for background processing - Returns a JSON response with the message ID --- ### ✔ 2. Queue Processing (Database Queue) A ProcessMessageJob performs: - Sanitization - Adding metadata (processed_at timestamp) - Broadcasting message.received event Run the queue worker:
bash
php artisan queue:work database
